image:
  imagePullSecrets: 
    - name: private-registry

bbtests:
  enabled: true

jira:
  resources:
    jvm:
      maxHeap: 768M
      minHeap: 384M
      reservedCodeCache: 512M
    container:
      requests:
        cpu: 1
        memory: 384M
 